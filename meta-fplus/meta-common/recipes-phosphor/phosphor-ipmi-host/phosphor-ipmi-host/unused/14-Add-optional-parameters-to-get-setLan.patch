From 60afe080a9ce0a81deba6b99ceb04b3154cb62cb Mon Sep 17 00:00:00 2001
From: Konstantin Klubnichkin <kitsok@yandex-team.ru>
Date: Mon, 21 Sep 2020 16:03:46 +0000
Subject: [PATCH 1/1] Add optional parameters to get/setLan

---
 transporthandler.cpp | 56 ++++++++++++++++++++++++++++++++++++++++++++++++++++
 transporthandler.hpp |  7 +++++++
 2 files changed, 63 insertions(+)

diff --git a/transporthandler.cpp b/transporthandler.cpp
index 77892e4..2274f68 100644
--- a/transporthandler.cpp
+++ b/transporthandler.cpp
@@ -1743,6 +1743,16 @@ RspType<> setLan(Context::ptr ctx, uint4_t channelBits, uint4_t reserved1,
                 return response(resp);
             }
         }
+        case LanParam::IP4Header:
+        case LanParam::BMCArpControl:
+        case LanParam::GratuitosArpInterval:
+        case LanParam::BackupGatewayAddress:
+        case LanParam::BackupGatewayMAC:
+        case LanParam::VLANPriority:
+        case LanParam::BadPasswordThreshold:
+        {
+            return responseSuccess();
+        }
     }
 
     if ((parameter >= oemCmdStart) && (parameter <= oemCmdEnd))
@@ -1863,6 +1873,23 @@ RspType<message::Payload> getLan(Context::ptr ctx, uint4_t channelBits,
             ret.pack(dataRef(netmask));
             return responseSuccess(std::move(ret));
         }
+        case LanParam::IP4Header:
+        {
+            ret.pack(static_cast<uint8_t>(0x40));
+            ret.pack(static_cast<uint8_t>(0x40));
+            ret.pack(static_cast<uint8_t>(0x10));
+            return responseSuccess(std::move(ret));
+        }
+        case LanParam::BMCArpControl:
+        {
+            ret.pack(static_cast<uint8_t>(0x02));
+            return responseSuccess(std::move(ret));
+        }
+        case LanParam::GratuitosArpInterval:
+        {
+            ret.pack(static_cast<uint8_t>(20));
+            return responseSuccess(std::move(ret));
+        }
         case LanParam::Gateway1:
         {
             auto gateway =
@@ -1882,6 +1909,20 @@ RspType<message::Payload> getLan(Context::ptr ctx, uint4_t channelBits,
             ret.pack(dataRef(mac));
             return responseSuccess(std::move(ret));
         }
+        case LanParam::BackupGatewayAddress:
+        {
+            auto gateway =
+                channelCall<getGatewayProperty<AF_INET>>(channel).value_or(
+                    in_addr{});
+            ret.pack(dataRef(gateway));
+            return responseSuccess(std::move(ret));
+        }
+        case LanParam::BackupGatewayMAC:
+        {
+            ether_addr mac{};
+            ret.pack(dataRef(mac));
+            return responseSuccess(std::move(ret));
+        }
         case LanParam::SNMPCommunity:
         {
             std::string communityString;
@@ -1916,6 +1957,11 @@ RspType<message::Payload> getLan(Context::ptr ctx, uint4_t channelBits,
             ret.pack(vlan);
             return responseSuccess(std::move(ret));
         }
+        case LanParam::VLANPriority:
+        {
+            ret.pack(static_cast<uint8_t>(0x00));
+            return responseSuccess(std::move(ret));
+        }
         case LanParam::CiphersuiteSupport:
         {
             if (getChannelSessionSupport(channel) ==
@@ -2058,6 +2104,16 @@ RspType<message::Payload> getLan(Context::ptr ctx, uint4_t channelBits,
                 return response(resp);
             }
         }
+        case LanParam::BadPasswordThreshold:
+        {
+            ret.pack(static_cast<uint8_t>(0x00));
+            ret.pack(static_cast<uint8_t>(0x00));
+            ret.pack(static_cast<uint8_t>(0x00));
+            ret.pack(static_cast<uint8_t>(0x00));
+            ret.pack(static_cast<uint8_t>(0x00));
+            ret.pack(static_cast<uint8_t>(0x00));
+            return responseSuccess(std::move(ret));
+        }
     }
 
     if ((parameter >= oemCmdStart) && (parameter <= oemCmdEnd))
diff --git a/transporthandler.hpp b/transporthandler.hpp
index d4d6ac6..39b00c6 100644
--- a/transporthandler.hpp
+++ b/transporthandler.hpp
@@ -18,13 +18,20 @@ enum class LanParam : uint8_t
     IPSrc = 4,
     MAC = 5,
     SubnetMask = 6,
+    IP4Header = 7,
+    BMCArpControl = 10,
+    GratuitosArpInterval = 11,
     Gateway1 = 12,
     Gateway1MAC = 13,
+    BackupGatewayAddress = 14,
+    BackupGatewayMAC = 15,
     SNMPCommunity = 16,
     VLANId = 20,
+    VLANPriority = 21,
     CiphersuiteSupport = 22,
     CiphersuiteEntries = 23,
     cipherSuitePrivilegeLevels = 24,
+    BadPasswordThreshold = 26,
     IPFamilySupport = 50,
     IPFamilyEnables = 51,
     IPv6Status = 55,
-- 
2.7.4

