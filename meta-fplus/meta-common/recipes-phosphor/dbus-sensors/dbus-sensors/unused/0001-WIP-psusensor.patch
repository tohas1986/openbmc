From 0023266b004afbc9da399835bb120925fc683165 Mon Sep 17 00:00:00 2001
From: Nikita Vedeneev <vedenena@yandex-team.ru>
Date: Wed, 7 Apr 2021 13:14:35 +0000
Subject: [PATCH] WIP-psusensor

---
 include/IpmbSensor.hpp |  14 ++---
 include/sensor.hpp     |   4 +-
 src/IpmbSensor.cpp     | 151 +++++++++++++++----------------------------------
 src/PSUSensor.cpp      |   2 +-
 src/PSUSensorMain.cpp  |   7 ++-
 5 files changed, 58 insertions(+), 120 deletions(-)

diff --git a/include/IpmbSensor.hpp b/include/IpmbSensor.hpp
index f354177..9d08ea9 100644
--- a/include/IpmbSensor.hpp
+++ b/include/IpmbSensor.hpp
@@ -1,7 +1,8 @@
 #pragma once
+#include "sensor.hpp"
+
 #include <boost/asio/steady_timer.hpp>
 #include <boost/container/flat_map.hpp>
-#include <sensor.hpp>
 
 #include <chrono>
 #include <limits>
@@ -38,7 +39,6 @@ enum class ReadingFormat
     byte3,
     elevenBit,
     elevenBitShift,
-    linearElevenBit,
     medimmsensor,
     cpu,
     dts,
@@ -54,7 +54,7 @@ constexpr uint8_t getSensorReading = 0x2d;
 
 static bool isValid(const std::vector<uint8_t>& data)
 {
-    constexpr auto readingUnavailableBit = 5;
+    constexpr auto ReadingUnavailableBit = 5;
 
     // Proper 'Get Sensor Reading' response has at least 4 bytes, including
     // Completion Code. Our IPMB stack strips Completion Code from payload so we
@@ -65,7 +65,7 @@ static bool isValid(const std::vector<uint8_t>& data)
     }
 
     // Per IPMI 'Get Sensor Reading' specification
-    if (data[1] & (1 << readingUnavailableBit))
+    if (data[1] & (1 << ReadingUnavailableBit))
     {
         return false;
     }
@@ -88,10 +88,9 @@ struct IpmbSensor : public Sensor
                const std::string& sensorConfiguration,
                sdbusplus::asio::object_server& objectServer,
                std::vector<thresholds::Threshold>&& thresholds,
-               uint8_t hostSMbusIndex,
                uint8_t& deviceAddress,  uint8_t& deviceCPUID, uint8_t& deviceChannelID,
                uint8_t& deviceDIMMID, std::string& deviceTypeName,std::string& sensorTypeName);
-    ~IpmbSensor() override;
+    ~IpmbSensor();
 
     void checkThresholds(void) override;
     void read(void);
@@ -106,7 +105,6 @@ struct IpmbSensor : public Sensor
     double scaleVal;
     double offsetVal;
     uint8_t commandAddress;
-    uint8_t netfn;
     uint8_t deviceCPUID = 0;
     uint8_t deviceChannelID = 0;
     uint8_t deviceDIMMID = 0;
@@ -115,10 +113,10 @@ struct IpmbSensor : public Sensor
                         0xff, 0x00, 0x00, 0x00, 0x00};
     std::vector<uint8_t> temp;
 
+    uint8_t netfn;
     uint8_t command;
     uint8_t deviceAddress;
     uint8_t errorCount;
-    uint8_t hostSMbusIndex;
     std::vector<uint8_t> commandData;
     std::optional<uint8_t> initCommand;
     std::vector<uint8_t> initData;
diff --git a/include/sensor.hpp b/include/sensor.hpp
index 0f90118..e6d4dc1 100644
--- a/include/sensor.hpp
+++ b/include/sensor.hpp
@@ -368,11 +368,11 @@ struct Sensor
         }
 
         errCount++;
-        if (errCount == errorThreshold)
+        /*if (errCount == errorThreshold)
         {
             std::cerr << "Sensor " << name << " reading error!\n";
             markFunctional(false);
-        }
+        }*/
     }
 
     void updateValue(const double& newValue)
diff --git a/src/IpmbSensor.cpp b/src/IpmbSensor.cpp
index 96faa9d..450b399 100644
--- a/src/IpmbSensor.cpp
+++ b/src/IpmbSensor.cpp
@@ -14,9 +14,15 @@
 // limitations under the License.
 */
 
-#include <IpmbSensor.hpp>
-#include <Utils.hpp>
-#include <VariantVisitors.hpp>
+#include "IpmbSensor.hpp"
+
+#include "Utils.hpp"
+#include "VariantVisitors.hpp"
+
+#include <math.h>
+#include <CLI/CLI.hpp>
+
+#include <boost/algorithm/string.hpp>
 #include <boost/algorithm/string/predicate.hpp>
 #include <boost/algorithm/string/replace.hpp>
 #include <boost/container/flat_map.hpp>
@@ -25,7 +31,6 @@
 #include <sdbusplus/bus/match.hpp>
 
 #include <chrono>
-#include <cmath>
 #include <functional>
 #include <iostream>
 #include <limits>
@@ -35,9 +40,8 @@
 #include <tuple>
 #include <variant>
 #include <vector>
-#include <CLI/CLI.hpp>
-
 #include <chrono>
+#include <cmath>
 
 bool DEBUG = false;
 
@@ -48,11 +52,12 @@ static constexpr double ipmbMinReading = 0;
 
 static constexpr uint8_t meAddress = 1;
 static constexpr uint8_t lun = 0;
-static constexpr uint8_t hostSMbusIndexDefault = 0x03;
 
 static constexpr const char* sensorPathPrefix = "/xyz/openbmc_project/sensors/";
 
 std::vector<uint8_t> meDataDIMMandCPU;
+static uint64_t globalTime = 0;
+static uint64_t readingMeDIMMDataTime = 0;
 double TjmaxValue = 100;
 int requestCnt = 0;
 
@@ -69,15 +74,13 @@ IpmbSensor::IpmbSensor(std::shared_ptr<sdbusplus::asio::connection>& conn,
                        const std::string& sensorConfiguration,
                        sdbusplus::asio::object_server& objectServer,
                        std::vector<thresholds::Threshold>&& thresholdData,
-                       uint8_t hostSMbusIndex,
                        uint8_t& deviceAddress, uint8_t& deviceCPUID, uint8_t& deviceChannelID,
                        uint8_t& deviceDIMMID, std::string& deviceTypeName,std::string& sensorTypeName) :
     Sensor(boost::replace_all_copy(sensorName, " ", "_"),
            std::move(thresholdData), sensorConfiguration,
            "xyz.openbmc_project.Configuration.ExitAirTemp", ipmbMaxReading,
            ipmbMinReading, conn, PowerState::on),
-    deviceAddress(deviceAddress), deviceCPUID(deviceCPUID), deviceChannelID(deviceChannelID), deviceDIMMID(deviceDIMMID), deviceTypeName(deviceTypeName), hostSMbusIndex(hostSMbusIndex),
-    objectServer(objectServer), waitTimer(io)
+    deviceAddress(deviceAddress), deviceCPUID(deviceCPUID), deviceChannelID(deviceChannelID), deviceDIMMID(deviceDIMMID), deviceTypeName(deviceTypeName),objectServer(objectServer), waitTimer(io)
 {
     std::string dbusPath = sensorPathPrefix + sensorTypeName + "/" + name;
 
@@ -193,14 +196,12 @@ void IpmbSensor::loadDefaults()
         command = ipmi::me_bridge::sendRawPmbus;
         initCommand = ipmi::me_bridge::sendRawPmbus;
         // pmbus read temp
-        commandData = {0x57,          0x01, 0x00, 0x16, hostSMbusIndex,
-                       deviceAddress, 0x00, 0x00, 0x00, 0x00,
-                       0x01,          0x02, 0x8d};
+        commandData = {0x57, 0x01, 0x00, 0x16, 0x3,  deviceAddress, 0x00,
+                       0x00, 0x00, 0x00, 0x01, 0x02, 0x8d};
         // goto page 0
-        initData = {0x57,          0x01, 0x00, 0x14, hostSMbusIndex,
-                    deviceAddress, 0x00, 0x00, 0x00, 0x00,
-                    0x02,          0x00, 0x00, 0x00};
-        readingFormat = ReadingFormat::linearElevenBit;
+        initData = {0x57, 0x01, 0x00, 0x14, 0x03, deviceAddress, 0x00,
+                    0x00, 0x00, 0x00, 0x02, 0x00, 0x00,          0x00};
+        readingFormat = ReadingFormat::elevenBit;
     }
     else if (type == IpmbType::IR38363VR)
     {
@@ -208,9 +209,8 @@ void IpmbSensor::loadDefaults()
         netfn = ipmi::me_bridge::netFn;
         command = ipmi::me_bridge::sendRawPmbus;
         // pmbus read temp
-        commandData = {0x57,          0x01, 0x00, 0x16, hostSMbusIndex,
-                       deviceAddress, 00,   0x00, 0x00, 0x00,
-                       0x01,          0x02, 0x8D};
+        commandData = {0x57, 0x01, 0x00, 0x16, 0x03, deviceAddress, 00,
+                       0x00, 0x00, 0x00, 0x01, 0x02, 0x8D};
         readingFormat = ReadingFormat::elevenBitShift;
     }
     else if (type == IpmbType::ADM1278HSC)
@@ -222,13 +222,9 @@ void IpmbSensor::loadDefaults()
             case IpmbSubType::curr:
                 uint8_t snsNum;
                 if (subType == IpmbSubType::temp)
-                {
                     snsNum = 0x8d;
-                }
                 else
-                {
                     snsNum = 0x8c;
-                }
                 netfn = ipmi::me_bridge::netFn;
                 command = ipmi::me_bridge::sendRawPmbus;
                 commandData = {0x57, 0x01, 0x00, 0x86, deviceAddress,
@@ -253,13 +249,11 @@ void IpmbSensor::loadDefaults()
         command = ipmi::me_bridge::sendRawPmbus;
         initCommand = ipmi::me_bridge::sendRawPmbus;
         // pmbus read temp
-        commandData = {0x57,          0x01, 0x00, 0x16, hostSMbusIndex,
-                       deviceAddress, 0x00, 0x00, 0x00, 0x00,
-                       0x01,          0x02, 0x8d};
+        commandData = {0x57, 0x01, 0x00, 0x16, 0x3,  deviceAddress, 0x00,
+                       0x00, 0x00, 0x00, 0x01, 0x02, 0x8d};
         // goto page 0
-        initData = {0x57,          0x01, 0x00, 0x14, hostSMbusIndex,
-                    deviceAddress, 0x00, 0x00, 0x00, 0x00,
-                    0x02,          0x00, 0x00, 0x00};
+        initData = {0x57, 0x01, 0x00, 0x14, 0x03, deviceAddress, 0x00,
+                    0x00, 0x00, 0x00, 0x02, 0x00, 0x00,          0x00};
         readingFormat = ReadingFormat::byte3;
     }
     else
@@ -322,6 +316,9 @@ bool IpmbSensor::processReading(const std::vector<uint8_t>& data, double& resp)
             }
 
             int16_t value = ((data[4] << 8) | data[3]);
+            constexpr const size_t shift = 16 - 11; // 11bit into 16bit
+            value <<= shift;
+            value >>= shift;
             resp = value;
             return true;
         }
@@ -340,67 +337,33 @@ bool IpmbSensor::processReading(const std::vector<uint8_t>& data, double& resp)
             resp = ((data[4] << 8) | data[3]) >> 3;
             return true;
         }
-        case (ReadingFormat::linearElevenBit):
-        {
-            if (data.size() < 5)
-            {
-                if (!errCount)
-                {
-                    std::cerr << "Invalid data length returned for " << name
-                              << "\n";
-                }
-                return false;
-            }
-
-            int16_t value = ((data[4] << 8) | data[3]);
-            constexpr const size_t shift = 16 - 11; // 11bit into 16bit
-            value <<= shift;
-            value >>= shift;
-            resp = value;
-            return true;
-        }
         case (ReadingFormat::medimmsensor):
         {
-            if(!ipmi::sensor::isValid(data))
-            {
-                return false;
-            }
-
             if (data.size() < 3)
             {
-                if (!errCount)
-                {
-                    std::cerr << "Invalid data length returned for " << name << "\n";
-                }
-
                 return false;
             }
-
             if(requestCnt > 24)
             {
                 requestCnt = 0;
             }
-
             if(meDataDIMMandCPU.size() >= (deviceDIMMID + deviceChannelID*4 + 3))
             {
                 resp = meDataDIMMandCPU[deviceDIMMID + deviceChannelID*4 + 3];
+                readingMeDIMMDataTime = std::chrono::duration_cast<std::chrono::seconds>(
+                std::chrono::system_clock::now().time_since_epoch()).count();
 
                 return true;
             }
-        }
-        case (ReadingFormat::tjmax):
-        {
-            if(!ipmi::sensor::isValid(data))
+            else
             {
                 return false;
             }
-
+        }
+        case (ReadingFormat::tjmax):
+        {
             if (data.size() < 3)
             {
-                if (!errCount)
-                {
-                    std::cerr << "Invalid data length returned for " << name << "\n";
-                }
                 return false;
             }
             TjmaxValue = data[0];
@@ -409,17 +372,8 @@ bool IpmbSensor::processReading(const std::vector<uint8_t>& data, double& resp)
         }
         case (ReadingFormat::dts):
         {
-            if(!ipmi::sensor::isValid(data))
-            {
-                return false;
-            }
-
             if (data.size() < 3)
             {
-                if (!errCount)
-                {
-                    std::cerr << "Invalid data length returned for " << name << "\n";
-                }
                 return false;
             }
             resp = data[3];
@@ -427,28 +381,20 @@ bool IpmbSensor::processReading(const std::vector<uint8_t>& data, double& resp)
         }
         case (ReadingFormat::cpu):
         {
-            if(!ipmi::sensor::isValid(data))
-            {
-                return false;
-            }
-
             if (data.size() < 3)
             {
-                if (!errCount)
-                {
-                    std::cerr << "Invalid data length returned for " << name << "\n";
-                }
                 return false;
             }
-            if (TjmaxValue > 0)
+            if (TjmaxValue > 0 && ipmi::sensor::isValid(data))
             {
                 resp = TjmaxValue - data[3];
                 return true;
-            } else
+            }
+            /*else if(ipmi::sensor::isValid(data))
             {
                 resp = 100 - data[3];
                 return true;
-            }
+            }*/
             return false;
         }
         default:
@@ -527,6 +473,7 @@ void IpmbSensor::read(void)
                     {
                         updateValue(value);
                     }
+
                     read();
                 },
                 "xyz.openbmc_project.Ipmi.Channel.Ipmb",
@@ -582,6 +529,7 @@ void IpmbSensor::read(void)
                 read();
                 return;
             }
+
             dbusConnection->async_method_call(
                 [this](boost::system::error_code ec,
                        const IpmbMethodType& response) {
@@ -738,6 +686,7 @@ void createSensors(
                         std::cerr << "error populating thresholds for " << name
                                   << "\n";
                     }
+
                     uint8_t deviceAddress =  0;
                     try
                     {
@@ -787,7 +736,7 @@ void createSensors(
                     {
                         deviceTypeName =
                             loadVariant<std::string>(entry.second, "TypeName");
-                        //std::cout << deviceTypeName << "\n";
+                        //std::cerr << deviceTypeName << "\n";
                     }
                     catch(...)
                     {
@@ -804,15 +753,6 @@ void createSensors(
                     {
                         std::cerr << "Unable to get Class, setting to default" << "\n";
                     }
-
-                    uint8_t hostSMbusIndex = hostSMbusIndexDefault;
-                    auto findSmType = entry.second.find("HostSMbusIndex");
-                    if (findSmType != entry.second.end())
-                    {
-                        hostSMbusIndex = std::visit(
-                            VariantToUnsignedIntVisitor(), findSmType->second);
-                    }
-
                     /* Default sensor type is "temperature" */
                     std::string sensorTypeName = "temperature";
                     auto findType = entry.second.find("SensorType");
@@ -825,9 +765,9 @@ void createSensors(
                     auto& sensor = sensors[name];
                     sensor = std::make_unique<IpmbSensor>(
                         dbusConnection, io, name, pathPair.first, objectServer,
-                        std::move(sensorThresholds), deviceAddress,
-                        hostSMbusIndex, deviceCPUID,
-                        deviceChannelID, deviceDIMMID, deviceTypeName, sensorTypeName);
+                        std::move(sensorThresholds), deviceAddress, deviceCPUID,
+                        deviceChannelID, deviceDIMMID, deviceTypeName,
+                        sensorTypeName);
 
                     /* Initialize scale and offset value */
                     sensor->scaleVal = 1;
@@ -955,7 +895,6 @@ void reinitSensors(sdbusplus::message::message& message)
             initCmdTimer->async_wait([](const boost::system::error_code ec) {
                 if (ec == boost::asio::error::operation_aborted)
                 {
-                    //return; // we're being canceled
                     std::cerr << "read: Error occured, exiting\n";
                     exit(-1);
                 }
@@ -1001,7 +940,7 @@ int main(int argc, char* argv[])
                 createSensors(io, objectServer, sensors, systemBus);
                 if (sensors.empty())
                 {
-                    std::cout << "Configuration not detected\n";
+                    std::cerr << "Configuration not detected\n";
                 }
             });
         };
diff --git a/src/PSUSensor.cpp b/src/PSUSensor.cpp
index ab6cd13..73505ab 100644
--- a/src/PSUSensor.cpp
+++ b/src/PSUSensor.cpp
@@ -71,7 +71,7 @@ PSUSensor::PSUSensor(const std::string& path, const std::string& objectType,
                              << path << "\n";
         inputDev.assign(fd);
     }
-    inputDev.assign(fd);
+    //inputDev.assign(fd);
 
     std::string dbusPath = sensorPathPrefix + sensorTypeName + name;
 
diff --git a/src/PSUSensorMain.cpp b/src/PSUSensorMain.cpp
index e8897b1..38137ed 100644
--- a/src/PSUSensorMain.cpp
+++ b/src/PSUSensorMain.cpp
@@ -41,7 +41,7 @@
 #include <CLI/CLI.hpp>
 bool DEBUG = false;
 
-static constexpr std::array<const char*, 37> sensorTypes = {
+static constexpr std::array<const char*, 38> sensorTypes = {
     "xyz.openbmc_project.Configuration.ADM1272",
     "xyz.openbmc_project.Configuration.ADM1278",
     "xyz.openbmc_project.Configuration.DPS800",
@@ -78,7 +78,8 @@ static constexpr std::array<const char*, 37> sensorTypes = {
     "xyz.openbmc_project.Configuration.JC42",
     "xyz.openbmc_project.Configuration.LM5066I",
     "xyz.openbmc_project.Configuration.LM5066I05",
-    "xyz.openbmc_project.Configuration.NCT7601"};
+    "xyz.openbmc_project.Configuration.NCT7601",
+    "xyz.openbmc_project.Configuration.dcdc54"};
 
 static std::vector<std::string> pmbusNames = {
     "adm1272",   "adm1278",   "dps800",    "ina219",   "ina230",   "isl68137",
@@ -86,7 +87,7 @@ static std::vector<std::string> pmbusNames = {
     "max20730",  "max20734",  "max20796",  "max34451", "pmbus",    "pxe1610",
     "raa228000", "raa228228", "raa229004", "tps546d24",
     "emc1413", "max31725", "max31730","max6581", "max6654", "tmp112", "tmp175",
-    "tmp421", "tmp441", "tmp75", "nct7802", "jc42", "lm5066i", "lm5066i05", "nct7601" };
+    "tmp421", "tmp441", "tmp75", "nct7802", "jc42", "lm5066i", "lm5066i05", "nct7601", "dcdc54" };
 
 namespace fs = std::filesystem;
 
-- 
2.7.4

