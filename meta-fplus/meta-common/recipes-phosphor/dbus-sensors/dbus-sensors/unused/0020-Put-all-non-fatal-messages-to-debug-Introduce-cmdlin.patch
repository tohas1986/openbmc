From bbbd8a3a55c598c8086a736fb3be35e2ed4ceed8 Mon Sep 17 00:00:00 2001
From: Konstantin Klubnichkin <kitsok@yandex-team.ru>
Date: Sat, 19 Sep 2020 14:28:38 +0000
Subject: [PATCH] Put all non-fatal messages to debug

---
 src/ADCSensorMain.cpp |  37 ++++++++++++-------
 src/PSUEvent.cpp      |   8 ++--
 src/PSUSensorMain.cpp | 100 +++++++++++++++++++++++---------------------------
 3 files changed, 74 insertions(+), 71 deletions(-)

diff --git a/src/ADCSensorMain.cpp b/src/ADCSensorMain.cpp
index d6a5133..a2b2938 100644
--- a/src/ADCSensorMain.cpp
+++ b/src/ADCSensorMain.cpp
@@ -18,6 +18,7 @@
 #include "Utils.hpp"
 #include "VariantVisitors.hpp"
 
+#include <CLI/CLI.hpp>
 #include <boost/algorithm/string/case_conv.hpp>
 #include <boost/algorithm/string/predicate.hpp>
 #include <boost/algorithm/string/replace.hpp>
@@ -36,7 +37,7 @@
 #include <variant>
 #include <vector>
 
-static constexpr bool DEBUG = false;
+bool DEBUG = false;
 
 namespace fs = std::filesystem;
 
@@ -54,7 +55,8 @@ bool isAdc(const fs::path& parentPath)
     std::ifstream nameFile(namePath);
     if (!nameFile.good())
     {
-        std::cerr << "Failure reading " << namePath.string() << "\n";
+        if (DEBUG)
+            std::cerr << "Failure reading " << namePath.string() << "\n";
         return false;
     }
 
@@ -133,8 +135,9 @@ void createSensors(
                     auto findIndex = baseConfiguration->second.find("Index");
                     if (findIndex == baseConfiguration->second.end())
                     {
-                        std::cerr << "Base configuration missing Index"
-                                  << baseConfiguration->first << "\n";
+                        if (DEBUG)
+                            std::cerr << "Base configuration missing Index"
+                                      << baseConfiguration->first << "\n";
                         continue;
                     }
 
@@ -152,23 +155,26 @@ void createSensors(
                 }
                 if (sensorData == nullptr)
                 {
-                    std::cerr << "failed to find match for " << path.string()
-                              << "\n";
+                    if (DEBUG)
+                        std::cerr << "failed to find match for " << path.string()
+                                  << "\n";
                     continue;
                 }
 
                 if (baseConfiguration == nullptr)
                 {
-                    std::cerr << "error finding base configuration for"
-                              << path.string() << "\n";
+                    if (DEBUG)
+                        std::cerr << "error finding base configuration for"
+                                  << path.string() << "\n";
                     continue;
                 }
 
                 auto findSensorName = baseConfiguration->second.find("Name");
                 if (findSensorName == baseConfiguration->second.end())
                 {
-                    std::cerr << "could not determine configuration name for "
-                              << path.string() << "\n";
+                    if (DEBUG)
+                        std::cerr << "could not determine configuration name for "
+                                  << path.string() << "\n";
                     continue;
                 }
                 std::string sensorName =
@@ -199,8 +205,9 @@ void createSensors(
                 std::vector<thresholds::Threshold> sensorThresholds;
                 if (!parseThresholdsFromConfig(*sensorData, sensorThresholds))
                 {
-                    std::cerr << "error populating thresholds for "
-                              << sensorName << "\n";
+                    if (DEBUG)
+                        std::cerr << "error populating thresholds for "
+                                  << sensorName << "\n";
                 }
 
                 auto findScaleFactor =
@@ -283,8 +290,12 @@ void createSensors(
         std::vector<std::string>{sensorTypes.begin(), sensorTypes.end()});
 }
 
-int main()
+int main(int argc, char* argv[])
 {
+    CLI::App app("ADC sensors poller");
+    app.add_option("-v,--verbose", DEBUG, "print debug output");
+    CLI11_PARSE(app, argc, argv);
+
     boost::asio::io_service io;
     auto systemBus = std::make_shared<sdbusplus::asio::connection>(io);
     systemBus->request_name("xyz.openbmc_project.ADCSensor");
diff --git a/src/PSUEvent.cpp b/src/PSUEvent.cpp
index 3eeb925..f47e1c1 100644
--- a/src/PSUEvent.cpp
+++ b/src/PSUEvent.cpp
@@ -32,6 +32,8 @@
 #include <variant>
 #include <vector>
 
+extern bool DEBUG;
+
 PSUCombineEvent::PSUCombineEvent(
     sdbusplus::asio::object_server& objectServer,
     std::shared_ptr<sdbusplus::asio::connection>& conn,
@@ -153,7 +155,7 @@ PSUSubEvent::PSUSubEvent(
     fd = open(path.c_str(), O_RDONLY);
     if (fd < 0)
     {
-        std::cerr << "PSU sub event failed to open file\n";
+        if (DEBUG) std::cerr << "PSU sub event failed to open file\n";
         return;
     }
     inputDev.assign(fd);
@@ -280,7 +282,7 @@ void PSUSubEvent::handleResponse(const boost::system::error_code& err)
     {
         if (errCount == warnAfterErrorCount)
         {
-            std::cerr << "Failure to read event at " << path << "\n";
+            if (DEBUG) std::cerr << "Failure to read event at " << path << "\n";
         }
         updateValue(0);
         errCount++;
@@ -417,7 +419,7 @@ void PSUSubEvent::beep(const uint8_t& beepPriority)
         [](boost::system::error_code ec) {
             if (ec)
             {
-                std::cerr << "beep error (ec = " << ec << ")\n";
+                if (DEBUG) std::cerr << "beep error (ec = " << ec << ")\n";
                 return;
             }
         },
diff --git a/src/PSUSensorMain.cpp b/src/PSUSensorMain.cpp
index b47e7a7..72ab328 100644
--- a/src/PSUSensorMain.cpp
+++ b/src/PSUSensorMain.cpp
@@ -273,7 +273,8 @@ void createSensors(boost::asio::io_service& io,
         std::ifstream nameFile(pmbusPath);
         if (!nameFile.good())
         {
-            std::cerr << "Failure finding pmbus path " << pmbusPath << "\n";
+            if (DEBUG)
+                std::cerr << "Failure finding pmbus path " << pmbusPath << "\n";
             continue;
         }
 
@@ -286,8 +287,9 @@ void createSensors(boost::asio::io_service& io,
         {
             // To avoid this error message, add your driver name to
             // the pmbusNames vector at the top of this file.
-            std::cerr << "Driver name " << pmbusName
-                      << " not found in sensor whitelist\n";
+            if (DEBUG)
+                std::cerr << "Driver name " << pmbusName
+                          << " not found in sensor whitelist\n";
             continue;
         }
 
@@ -297,7 +299,7 @@ void createSensors(boost::asio::io_service& io,
         auto ret = directories.insert(directory.string());
         if (!ret.second)
         {
-            std::cerr << "Duplicate path " << directory.string() << "\n";
+            if (DEBUG) std::cerr << "Duplicate path " << directory.string() << "\n";
             continue; // check if path has already been searched
         }
 
@@ -306,7 +308,7 @@ void createSensors(boost::asio::io_service& io,
         auto findHyphen = deviceName.find("-");
         if (findHyphen == std::string::npos)
         {
-            std::cerr << "found bad device" << deviceName << "\n";
+            if (DEBUG) std::cerr << "found bad device" << deviceName << "\n";
             continue;
         }
         std::string busStr = deviceName.substr(0, findHyphen);
@@ -322,8 +324,9 @@ void createSensors(boost::asio::io_service& io,
         }
         catch (std::invalid_argument&)
         {
-            std::cerr << "Error parsing bus " << busStr << " addr " << addrStr
-                      << "\n";
+            if (DEBUG)
+                std::cerr << "Error parsing bus " << busStr << " addr " << addrStr
+                          << "\n";
             continue;
         }
 
@@ -351,8 +354,9 @@ void createSensors(boost::asio::io_service& io,
             }
             if (baseConfig == nullptr)
             {
-                std::cerr << "error finding base configuration for "
-                          << deviceName << "\n";
+                if (DEBUG)
+                    std::cerr << "error finding base configuration for "
+                              << deviceName << "\n";
                 continue;
             }
 
@@ -362,7 +366,8 @@ void createSensors(boost::asio::io_service& io,
             if (configBus == baseConfig->second.end() ||
                 configAddress == baseConfig->second.end())
             {
-                std::cerr << "error finding necessary entry in configuration\n";
+                if (DEBUG)
+                    std::cerr << "error finding necessary entry in configuration\n";
                 continue;
             }
 
@@ -371,8 +376,8 @@ void createSensors(boost::asio::io_service& io,
             if (!(confBus = std::get_if<uint64_t>(&(configBus->second))) ||
                 !(confAddr = std::get_if<uint64_t>(&(configAddress->second))))
             {
-                std::cerr
-                    << "Cannot get bus or address, invalid configuration\n";
+                if (DEBUG)
+                    std::cerr << "Cannot get bus or address, invalid configuration\n";
                 continue;
             }
 
@@ -387,7 +392,7 @@ void createSensors(boost::asio::io_service& io,
             std::vector<thresholds::Threshold> confThresholds;
             if (!parseThresholdsFromConfig(*sensorData, confThresholds))
             {
-                std::cerr << "error populating totoal thresholds\n";
+                if (DEBUG) std::cerr << "error populating totoal thresholds\n";
             }
             thresholdConfSize = confThresholds.size();
 
@@ -398,7 +403,7 @@ void createSensors(boost::asio::io_service& io,
         {
             // To avoid this error message, add your export map entry,
             // from Entity Manager, to sensorTypes at the top of this file.
-            std::cerr << "failed to find match for " << deviceName << "\n";
+            if (DEBUG) std::cerr << "failed to find match for " << deviceName << "\n";
             continue;
         }
 
@@ -412,7 +417,7 @@ void createSensors(boost::asio::io_service& io,
 
         if (!(psuName = std::get_if<std::string>(&(findPSUName->second))))
         {
-            std::cerr << "Cannot find psu name, invalid configuration\n";
+            if (DEBUG) std::cerr << "Cannot find psu name, invalid configuration\n";
             continue;
         }
         checkEvent(directory.string(), eventMatch, eventPathList);
@@ -432,17 +437,14 @@ void createSensors(boost::asio::io_service& io,
         std::vector<fs::path> sensorPaths;
         if (!findFiles(directory, R"(\w\d+_input$)", sensorPaths, 0))
         {
-            std::cerr << "No PSU non-label sensor in PSU\n";
+            if (DEBUG) std::cerr << "No PSU non-label sensor in PSU\n";
             continue;
         }
 
         /* read max value in sysfs for in, curr, power, temp, ... */
         if (!findFiles(directory, R"(\w\d+_max$)", sensorPaths, 0))
         {
-            if (DEBUG)
-            {
-                std::cerr << "No max name in PSU \n";
-            }
+            if (DEBUG) std::cerr << "No max name in PSU \n";
         }
 
         /* Find array of labels to be exposed if it is defined in config */
@@ -472,8 +474,9 @@ void createSensors(boost::asio::io_service& io,
             }
             else
             {
-                std::cerr << "Could not extract the alpha prefix from "
-                          << sensorNameStr;
+                if (DEBUG)
+                    std::cerr << "Could not extract the alpha prefix from "
+                              << sensorNameStr;
                 continue;
             }
 
@@ -538,10 +541,8 @@ void createSensors(boost::asio::io_service& io,
             }
 
             if (DEBUG)
-            {
                 std::cerr << "Sensor type=\"" << sensorNameSubStr
                           << "\" label=\"" << labelHead << "\"\n";
-            }
 
             checkPWMSensor(sensorPath, labelHead, *interfacePath,
                            dbusConnection, objectServer, psuNames[0]);
@@ -565,10 +566,8 @@ void createSensors(boost::asio::io_service& io,
             if (findProperty == labelMatch.end())
             {
                 if (DEBUG)
-                {
                     std::cerr << "Could not find matching default property for "
                               << labelHead << "\n";
-                }
                 continue;
             }
 
@@ -597,7 +596,7 @@ void createSensors(boost::asio::io_service& io,
                 }
                 catch (std::invalid_argument&)
                 {
-                    std::cerr << "Unable to parse " << keyName << "\n";
+                    if (DEBUG) std::cerr << "Unable to parse " << keyName << "\n";
                     continue;
                 }
 
@@ -616,7 +615,7 @@ void createSensors(boost::asio::io_service& io,
                 }
                 catch (std::invalid_argument&)
                 {
-                    std::cerr << "Unable to parse " << keyScale << "\n";
+                    if (DEBUG) std::cerr << "Unable to parse " << keyScale << "\n";
                     continue;
                 }
 
@@ -627,7 +626,7 @@ void createSensors(boost::asio::io_service& io,
                 }
                 else
                 {
-                    std::cerr << "Unable to accept " << keyScale << "\n";
+                    if (DEBUG) std::cerr << "Unable to accept " << keyScale << "\n";
                     continue;
                 }
             }
@@ -642,7 +641,7 @@ void createSensors(boost::asio::io_service& io,
                 }
                 catch (std::invalid_argument&)
                 {
-                    std::cerr << "Unable to parse " << keyMin << "\n";
+                    if (DEBUG) std::cerr << "Unable to parse " << keyMin << "\n";
                     continue;
                 }
             }
@@ -657,14 +656,14 @@ void createSensors(boost::asio::io_service& io,
                 }
                 catch (std::invalid_argument&)
                 {
-                    std::cerr << "Unable to parse " << keyMax << "\n";
+                    if (DEBUG) std::cerr << "Unable to parse " << keyMax << "\n";
                     continue;
                 }
             }
 
             if (!(psuProperty->minReading < psuProperty->maxReading))
             {
-                std::cerr << "Min must be less than Max\n";
+                if (DEBUG) std::cerr << "Min must be less than Max\n";
                 continue;
             }
 
@@ -697,19 +696,18 @@ void createSensors(boost::asio::io_service& io,
 
                 if (psuNames.size() <= nameIndex)
                 {
-                    std::cerr << "Could not pair " << labelHead
-                              << " with a Name field\n";
+                    if (DEBUG)
+                        std::cerr << "Could not pair " << labelHead
+                                  << " with a Name field\n";
                     continue;
                 }
 
                 psuNameFromIndex = psuNames[nameIndex];
 
                 if (DEBUG)
-                {
                     std::cerr << "Sensor label head " << labelHead
                               << " paired with " << psuNameFromIndex
                               << " at index " << nameIndex << "\n";
-                }
             }
 
             checkEventLimits(sensorPathStr, limitEventMatch, eventPathList);
@@ -738,36 +736,34 @@ void createSensors(boost::asio::io_service& io,
                 }
 
                 if (DEBUG)
-                {
                     std::cerr << "Sensor scaling factor " << factor
                               << " string " << strScaleFactor << "\n";
-                }
             }
 
             std::vector<thresholds::Threshold> sensorThresholds;
             if (!parseThresholdsFromConfig(*sensorData, sensorThresholds,
                                            &labelHead))
             {
-                std::cerr << "error populating thresholds for "
-                          << sensorNameSubStr << "\n";
+                if (DEBUG)
+                    std::cerr << "error populating thresholds for "
+                              << sensorNameSubStr << "\n";
             }
 
             auto findSensorType = sensorTable.find(sensorNameSubStr);
             if (findSensorType == sensorTable.end())
             {
-                std::cerr << sensorNameSubStr
-                          << " is not a recognized sensor type\n";
+                if (DEBUG)
+                    std::cerr << sensorNameSubStr
+                              << " is not a recognized sensor type\n";
                 continue;
             }
 
             if (DEBUG)
-            {
                 std::cerr << "Sensor properties: Name \""
                           << psuProperty->labelTypeName << "\" Scale "
                           << psuProperty->sensorScaleFactor << " Min "
                           << psuProperty->minReading << " Max "
                           << psuProperty->maxReading << "\n";
-            }
 
             std::string sensorName = psuProperty->labelTypeName;
             if (customizedName)
@@ -776,7 +772,7 @@ void createSensors(boost::asio::io_service& io,
                 {
                     // Allow selective disabling of an individual sensor,
                     // by customizing its name to an empty string.
-                    std::cerr << "Sensor disabled, empty string\n";
+                    if (DEBUG) std::cerr << "Sensor disabled, empty string\n";
                     continue;
                 }
             }
@@ -789,11 +785,9 @@ void createSensors(boost::asio::io_service& io,
             }
 
             if (DEBUG)
-            {
                 std::cerr << "Sensor name \"" << sensorName << "\" path \""
                           << sensorPathStr << "\" type \"" << sensorType
                           << "\"\n";
-            }
 
             std::optional<BridgeGpio> bridgeGpio;
             for (const SensorBaseConfiguration& suppConfig : *sensorData)
@@ -847,9 +841,7 @@ void createSensors(boost::asio::io_service& io,
             sensors[sensorName]->setupRead();
             ++numCreated;
             if (DEBUG)
-            {
                 std::cerr << "Created " << numCreated << " sensors so far\n";
-            }
         }
 
         // OperationalStatus event
@@ -860,10 +852,8 @@ void createSensors(boost::asio::io_service& io,
                 groupEventPathList, "OperationalStatus");
     }
 
-    if (DEBUG)
-    {
-        std::cerr << "Created total of " << numCreated << " sensors\n";
-    }
+    if (DEBUG) std::cerr << "Created total of " << numCreated << " sensors\n";
+
     return;
 }
 
@@ -969,7 +959,7 @@ int main(int argc, char* argv[])
         [&](sdbusplus::message::message& message) {
             if (message.is_method_error())
             {
-                std::cerr << "callback method error\n";
+                if (DEBUG) std::cerr << "callback method error\n";
                 return;
             }
             filterTimer.expires_from_now(boost::posix_time::seconds(3));
@@ -980,7 +970,7 @@ int main(int argc, char* argv[])
                 }
                 else if (ec)
                 {
-                    std::cerr << "timer error\n";
+                    if (DEBUG) std::cerr << "timer error\n";
                 }
                 createSensors(io, objectServer, systemBus);
             });
